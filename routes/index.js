"use strict"

const Router = require('koa-router');
const router = new Router();
const logger = require('../libs/logger');
const common = require('../libs/common');

// router.get('/', ctx => {
// //  this.redirect("/dist/pc/login.html");
//     ctx.body = "Welcom to here !";
// });

module.exports = router;